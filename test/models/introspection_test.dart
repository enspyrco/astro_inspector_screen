import 'package:flutter_test/flutter_test.dart';

void main() {
  test('InspectorState.addMissionReport throws on invalid input', () {
    // var state = IntrospectionBeliefs.initial;
    // expect(() => state.addMissionReport({}),
    //     throwsA('added json has no `cognition` key'));
    // expect(() => cognitiveProcesses.add({'cognition': 'blah'}),
    // throwsA(isA<_TypeError>()));
  });
}
